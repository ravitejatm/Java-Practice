package practice.Oops.Encapsulation;

public class Encapsulation {
    //encapsulation is a mechanism of wrapping the data(variables) and code acting on the data(methods) together as asingle unit.
    //encapsulation describes the ability of an object to hide its data and methods from the rest of the world.
    // data(fields, properties veriables) and code(methods, behaviour, action) as single unit(class)
    //EXAMPLE:- class or object
    //to secure data not to provide direct access.
    // only througth methods we provide access.
}
